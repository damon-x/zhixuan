工作区
报告生成
报告保存知识库

> 支持 微信 发送和回复图片

> 集成表情包 ， https://www.dogetu.com/search.html

python , cli 等执行和沙箱环境安全隔离
pg 集成
客户端
表格上传和 duckdb 数据分析
数据拉取 api 接入， 
计划梳理和推进，作为一个垂直模块来处理。帮助计划整理，待办提醒，监督，督促。卡点询问。独立记忆，比如用户偷偷删除了待办，但ai记下了
找一些做计划的方法论

有些 tool 可能必须是在某一场景下才需要生效的，比如调整计划的一些列 待办，定时任务相关的 tool 甚至一些后台管理用的tool, 可否平时不放在上下文，在有需要时批量加载出来，在按需调用

自我感知，就是知道自己有哪些能力，比如自身的操作文档

> HITL 复杂表单，快捷跳转到站内页面，站外页面

设置集成到对话中，比如对话中配置IM

资讯关注和报告，提供 webfetch tool , 集成 jina 网页内容获取 api,weh search / fetch api : https://www.shuyanai.com/price
离线任务，比如大的数据分析任务

超拟人模式，真人对话模拟和调试,人格切换，可以切换，定制回答风格，比如系统提示词，预制风格配置模板，如最大回复字数，场景+回复风格

用户主动停止任务后的上下文处理，消息删除、停止标记注入

模糊记忆，对过往聊过的内容，用户的基本特点总结为常驻记忆。为了避免模型过度强调它知道什么记忆，可以让常驻的记忆更模糊一点，比如知道用户在辽宁，但常驻记忆中只说用户在北方，也要做提示如果需要知道具体信息的话可以搜索记忆。另外，如果把过往聊过的东西都总结出来，可以稀释对用户一些特定记忆的注意力，比如性别，学历等，避免回答中出现生硬的记忆关联

历史对话的引用功能，可以前端做个引用的标记，后端做个读取对话记录的 tool 

ui 优化：聊天记录窄一点， 输入框和发送按钮都往中间聚拢一点

web 前端显示 tool 调用记录， 可以不渲染之前的历史tool 记录， 就当前这次打开web 页面过程中收到的 toll call 的出入参显示出来。 agent 循环中需要把 tool call 信息吐出来， 可能要改对话请求模式 ， 比如长连接




---
已完成
im 接入，qq
llm api 等配置化
先实现定时任务，主动提醒的基础功能，再做计划的功能
集成微信
模型自动切换
知识库解析图片
web 回复图片
立刻停止按钮和斜杠命令
支持web聊天页面发送和回复图片
集成文件读写改的tool
token 日志
web 聊天页面点击图片放大查看
统一资源标识[资源类型@资源名称:资源路径]
对话中支持图片和文字一起发送
支持 qq 发送和回复图片
用户偏好记忆和加载
研究让 agent 在合适的时候记录信息
缓存优化：动态系统提示词构建？如知识库切换？
搞一个获取环境信息的 tool ,让 agent 获取当前时间，用户id,sessionid，知识库列表 等上下文时间，可以让 llm 自己调用这个tool ,也可以伪造上下文中 tool 记录，来在相对靠后的位置注入一些经常需要变化的上下文信息，只是这样的问题就是只能追加信息，二不能删除无关上下文呢。或者永远伪造这个tool 记录，永远放在末尾，而不是混在对话记录中


任务规划，比如用户有一个30日的学习机器学习的规划，那么这个计划作为一个专题，所有相关的计划，心得笔记，待办，进度，结论等都统一维护
  新建计划
  聊聊想法
  生成规划，待办，定时任务，开始时间，结束时间
  完成会话
  每日跟踪待办，计划微调，询问感想
  支持页面手动调整，支持对话调整
样式重构：配色，多列展示，
记忆系统搭建
  旁路记忆系统

通知优化， 现在的定时任务中如果 agent 通过 tool 调用qq通知了用户， 有几点问题：1, agent 对话中有 “执行xxx 任务"的 ， user 信息记录. 2, agent 发送的qq提醒在 web 端看不到。 3. agent 发送的信息在 tool call 中， 不会落库， agent 以后不知道自己做过这件事，用户追问也可能会有问题。 

优化页面消息输入和填写交互
对话记录显示时间

定时任务，可以手动在页面配置，也可以通过对话去控制，比如，每晚提醒人写日记，还可以让ai主动去检查用户的待办是否完成？

做一个轻量的类似 skill 系统， skill 的本质是动态的提示词注入， 可以在页面上搞一个列表，维护若干条提示词，可以开关，这样比如让 agent 记住当用户说今天的运动量时就记录到哪里，怎么记，这样就可以稳定的触发一些功能。 提示词可以有摘要和详情，如果是一些复杂的提示词还可以放在详情里渐进式加载，哪些自己创建数据库的tool 也可以渐进式加载

集成 体重管理，记账，日记，心情，本质上这些都属于记录，这些领域的垂类 app 做的最核心的事就是通过好的交互设计，让用户方便地轻松地记录这些东西。
增加一个tool ,当用户谈论自己的心情，个人信息，购物，重要事件时，默默记下来，梳理良好的数据结构和打标体系

加载记忆，每用户消息走固定流程：用户消息原文作为query 参数 ， 搜索记忆，结果放入 放入末尾伪造的 tool call 里, 记忆列表要保留，可以持久化，下一次增量更新，做去重。 另外对话agent 内补充 search memary tool , 便于llm 自己有可能想调用， 当上下文剪裁或者压缩时，根据剪裁后的用户消息全量刷新相关记忆
对话中途前端关闭，重新打开会话后继续开始
优化记忆agent 的上下文和工作流
优化前端的记忆管理交互
文件预览， txt ,md , pdf , 图片，html
会话压缩，因为主要是聊天等简单任务，上下文可以维护小一点，可以提高模型回复速度
消息加时间，类似 “[2026-06-30 21:32]hi” , 只是发给llm 时拼接上，其它任何场景都不拼接 